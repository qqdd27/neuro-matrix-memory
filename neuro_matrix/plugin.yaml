name: neuromatrix
version: 0.6.0
description: "NeuroMatrix memory — local-first temporal knowledge-graph provider: cross-session entity IDs, alias merging, weighted decaying associations, sleep-style consolidation into dossiers (SQLite+FTS5, LLM-optional)."
hooks:
  - on_session_end
  - on_pre_compress
  - on_memory_write
